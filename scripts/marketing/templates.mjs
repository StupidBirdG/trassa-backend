// Детерминированные шаблоны постов — без вызова LLM (Anthropic API ключ, привязанный
// к прод-аккаунту, не имеет баланса на прямые Messages-запросы, см. PRODUCTION-STATE.md
// п.49). Бесплатно, без внешних зависимостей, всегда работает. Меняется только
// комбинация HOOK[angleIndex] + FACT (из facts-angles.mjs) + CTA[angleIndex] —
// 10 углов × 10 фактов = 100 разных постов по детерминированному дневному циклу.

const HOOKS = [
  'Машина простаивает между рейсами?',
  'Обзванивать десяток транспортных компаний вручную — так себе способ найти перевозчика.',
  'Отдавать груз незнакомой компании всегда немного страшно.',
  'Раньше это занимало часы. Теперь — минуты.',
  'Никто не хочет ни переплатить, ни продешевить.',
  'Задайте себе один вопрос: сколько вы теряете на пустых рейсах или обзвонах?',
  'Раньше — звонки наугад. Теперь — предложения сами приходят.',
  'Директору транспортной компании: у вашего автопарка наверняка есть окна между заказами.',
  'Мебель, стройматериалы, продукты — нерегулярные грузы тоже нужно куда-то возить.',
  'Контроль над сделкой — это не звонки каждые полчаса.',
];

const CTAS = [
  'Заявки от грузовладельцев по всей стране — забронируйте окно между рейсами.',
  'Разместите груз бесплатно, перевозчики сами предложат цену.',
  'Прозрачность вместо доверия на слово — вот что с этим делает Trassa.',
  'Вот что конкретно меняется с Trassa:',
  'Вот как Trassa убирает эту неопределённость:',
  'Ответ есть в Trassa:',
  'Так теперь работает Trassa:',
  'Вот предложение для вашего автопарка:',
  'Вот что подходит именно для таких грузов:',
  'Вот что даёт контроль без лишних звонков:',
];

const HASHTAG_POOL = [
  ['#грузоперевозки', '#логистика', '#казахстан', '#trassa'],
  ['#грузоперевозкиказахстан', '#доставкагрузов', '#логистикаkz'],
  ['#доверие', '#безопаснаясделка', '#грузоперевозки'],
  ['#логистика', '#бизнескз', '#эффективность'],
  ['#грузоперевозки', '#ценаперевозки', '#прозрачность'],
  ['#trassa', '#грузоперевозкиказахстан', '#логистика'],
  ['#доставка', '#грузы', '#казахстан'],
  ['#автопарк', '#b2bлогистика', '#перевозчикам'],
  ['#малыйбизнескз', '#доставкатоваров', '#логистика'],
  ['#gps', '#трекинггруза', '#логистикакз'],
];

export function buildChannelPost(fact, angleIndex) {
  const hook = HOOKS[angleIndex];
  const cta = CTAS[angleIndex];
  return `${hook}\n\n${cta} ${fact}.\n\n🚛 trassakz.com`;
}

export function buildInstagramCaption(fact, angleIndex, factIndex) {
  const hook = HOOKS[angleIndex];
  const cta = CTAS[angleIndex];
  const hashtags = HASHTAG_POOL[factIndex].join(' ');
  return `${hook}\n\n${cta} ${fact}.\n\n📲 trassakz.com\n\n${hashtags}\n\n[Кадр: скриншот ленты грузов в приложении Trassa на телефоне]`;
}

export function buildReelsScript(fact, angleIndex) {
  const hook = HOOKS[angleIndex];
  return `Хук (0-2с): текст на экране — "${hook}"
0-2с: экранкаст телефона — открывается лента грузов в приложении Trassa
3-10с: палец скроллит карточки грузов, видно города/маршруты, субтитр — "${fact}"
10-18с: крупный план на кнопку отклика / нужный элемент интерфейса, подтверждающий этот факт
18-25с: текст на экране — "trassakz.com", логотип/иконка приложения
(Читается без звука — весь смысл в субтитрах на экране, никакой постановки с актёрами)`;
}
